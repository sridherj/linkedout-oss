# Default env vars for unit tests. Loaded by conftest.py via load_dotenv(override=False).
# CI env vars and .env.local take precedence over these values.
LINKEDOUT_ENVIRONMENT=test
DATABASE_URL=postgresql://linkedout:test@localhost:5432/linkedout_test
