# Default env vars for integration tests. Loaded by integration conftest via load_dotenv(override=False).
# CI env vars and .env.local take precedence over these values.
LINKEDOUT_ENVIRONMENT=integration_test
LINKEDOUT_EMBEDDING__PROVIDER=local
