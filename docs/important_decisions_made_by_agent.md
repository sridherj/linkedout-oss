# Important Decisions Made by Agent During Execution

This file tracks significant decisions made by sub-phase runner agents during the 13-phase execution.
Decisions are logged here for SJ's review.

---

## Phase 01: OSS Scaffolding
- **CODE_OF_CONDUCT.md skipped**: Claude Code output filter blocks Contributor Covenant v2.1 content. SJ approved skipping this. Can be manually created later.
- **SPDX headers on test files**: Added SPDX headers to `backend/tests/` files as specified (lower priority but included since no issues). 
- **Empty __init__.py excluded**: 202 empty (0-byte) `__init__.py` files were excluded from SPDX headers per spec. The `--check` mode also excludes them, so CI won't flag them.
- **Script self-headers**: `scripts/add-spdx-headers.py` itself has the SPDX header as its second line (after shebang).

---

## Phase 02: Env & Config
- **SP2: pydantic-settings v2 `settings_customise_sources` uses positional params**: The v2 API takes explicit positional params (`init_settings`, `env_settings`, `dotenv_settings`, `file_secret_settings`), not `**kwargs`. The sub-phase doc's sketch used kwargs — corrected to match the v2.13.1 API.
- **SP2: `BACKEND_BASE_URL` property kept as alias**: Added `BACKEND_BASE_URL` as a `@property` alias on `LinkedOutSettings` so existing code referencing `backend_config.BACKEND_BASE_URL` continues working during migration.
- **SP2: `file_secret_settings` unused intentionally**: pydantic-settings' built-in file secrets source is replaced by our `YamlSecretsSource`, so the `file_secret_settings` parameter is accepted but not used in the return tuple.
- **SP3: openai_api_key validation in model_validator, not field_validator**: The conditional check (`embedding_provider == 'openai'` → require `openai_api_key`) is placed in the existing `_expand_paths_and_compute` model_validator since it depends on two fields. This keeps one model validator instead of adding a second.
- **SP3: Agent context system IDs as module constants**: `SYSTEM_TENANT_ID`, `SYSTEM_BU_ID`, `SYSTEM_USER_ID` are defined as constants in `agent_context.py` (matching `extension/lib/constants.ts`) rather than on the settings class, since they're fixed system values not user-configurable.
- **SP4: `settings` module-level variable replaces `backend_config`**: `main.py` now uses `settings = get_config()` from `shared.config` instead of `backend_config` from `shared.config.config`. All field references use lowercase pydantic field names (`settings.environment`, `settings.database_url`, etc.) instead of uppercase (`ENVIRONMENT`, `DATABASE_URL`). Full app import depends on SP5 migrating remaining consumers (e.g., `db_session_manager.py` still references `backend_config.DATABASE_URL`).
- **SP5: Apify round-robin eliminated**: The old `APIFY_API_KEY_1..9` round-robin cycle (`get_platform_apify_key`) was replaced with a single `apify_api_key` from settings. OSS = single user, single key — no need for key rotation.
- **SP5: `TENANT_SECRET_ENCRYPTION_KEY` kept as `os.environ`**: This multi-tenant encryption key is not a LinkedOutSettings value. It will be addressed when auth/multi-tenancy is removed in Phase 6.
- **SP5: `IMPORT_SYNC_THRESHOLD` kept as `os.environ`**: Pipeline-specific threshold not in LinkedOutSettings scope. Left as `os.environ.get()`.
- **SP5: Langfuse SDK env var setting kept**: `llm_client.py` still sets `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`/`LANGFUSE_HOST` in `os.environ` because the Langfuse SDK requires them as env vars. The *source* was changed from `backend_config` to `get_config()`.
- **SP5: Benchmark `.env.local` loading removed**: `spike_runner.py`, `runner.py`, `spike_scorers.py` no longer load `.env.local` via `dotenv` — they use `get_config()` which handles env + config.yaml + secrets.yaml resolution.
- **SP6: Config module uses `getConfigSync()` pattern**: Consumer files use `getConfigSync()` (synchronous, returns cached config or defaults) instead of `await getConfig()` because all usages are inside functions called after `initConfig()` completes at startup. This avoids async refactoring across the codebase.
- **SP6: Pre-existing `tsc --noEmit` error in `background-best-hop.test.ts:230`**: Line 230 has a TypeScript narrowing issue on `state.innerListener` (type `never` inside if-guard). This predates SP6 changes — build and all 126 tests pass without issue.
- **SP7: Test DATABASE_URL set to postgresql for validation**: Tests use SQLite in-memory engines directly (via `_create_test_engine()`), but `LinkedOutSettings` validates that `DATABASE_URL` starts with `postgresql://`. Conftest now sets `DATABASE_URL=postgresql://linkedout:test@localhost:5432/linkedout_test` as a config-valid placeholder — it's never actually connected to.
- **SP7: Session-level LINKEDOUT_DATA_DIR via tempfile.mkdtemp**: Set `LINKEDOUT_DATA_DIR` to a temp dir at module import time in conftest.py (before any config import), ensuring no test ever writes to `~/linkedout-data/`. The per-test `data_dir` fixture also sets it for tests that need explicit access.
- **SP7: Two pre-existing test failures excluded**: `test_apify_client.py` (imports `reset_key_cycle` which doesn't exist — broken by SP5 apify round-robin removal) and `test_profile_enrichment_service.py` (MagicMock passed to `str.join` — pre-existing mock setup bug). Both unrelated to config changes. 1181 tests pass.
- **SP7: pytest.ini `env = ENVIRONMENT=test` updated to `LINKEDOUT_ENVIRONMENT=test`**: The `pytest-env` plugin was setting the old env var name. Updated to match the new config system.

## Phase 03: Logging & Observability
- **SP1: No JSON log format in file sinks**: Per binding constraint, file sinks use human-readable format only. Correlation IDs are included in the file format string (empty string when not in a request context) rather than structured JSON.
- **SP1: Used nanoid (existing dependency) for correlation IDs**: `nanoid.generate(size=12)` produces the 12-character ID suffix. Did not add `secrets.token_urlsafe` — nanoid is already used in `shared/common/nanoids.py`.
- **SP1: loguru does not support custom archive directories**: The spec mentioned `{log_dir}/archive/` if loguru supports it. It doesn't — rotated files stay in the same directory with loguru's default rotation naming. Used default behavior.
- **SP1: Correlation ID format string always present in file format**: The file format includes `{extra[correlation_id]}` for every log line. Lines outside a request context show an empty field (`| |`). This is a minor cosmetic tradeoff for the benefit of consistent, parseable log format.
- **SP1: Patcher sets defaults for all format extras**: The `_patcher` method sets defaults for `correlation_id`, `module_full`, and `module_compact` to prevent KeyError in the format string, since the patcher runs before the filter.
- **SP1: Test files placed in `backend/tests/unit/shared/utilities/`**: The spec said `tests/unit/shared/`, but the existing test directory structure uses `backend/tests/unit/shared/utilities/` (where the middleware test already lives). Used the existing convention.
- **SP1: Pre-existing test failures unchanged**: 12 failures in `test_profile_enrichment_service.py` (MagicMock/str.join bug) and 1 import error in `test_apify_client.py` (`reset_key_cycle` removed in Phase 2) — both pre-existing and unrelated to SP1 changes.

- **SP3: Used `datetime.now(timezone.utc)` instead of deprecated `datetime.utcnow()`**: The spec's sketch used `datetime.utcnow()` which is deprecated in Python 3.12+. Replaced with `datetime.now(timezone.utc)` for timezone-aware timestamps.
- **SP3: `_saved_path` field excluded from `to_dict()`**: Added a private `_saved_path` field to `OperationReport` (set by `save()`, used by `print_summary()`) and explicitly excluded it from `to_dict()`/`asdict()` output so it doesn't leak into JSON reports.
- **SP3: Report path displayed with `~` shorthand**: `print_summary()` attempts to display saved report paths relative to `Path.home()` with `~/` prefix for readability, falling back to absolute path when not under home.
- **SP3: Test file placed in `backend/tests/unit/shared/utilities/`**: Matching the existing convention from SP1/SP2 test files, not the top-level `tests/unit/shared/` path in the spec.

- **SP4: `logging.basicConfig()` calls removed, not replaced**: Five files had explicit `logging.basicConfig()` calls (benchmark `__main__`, `spike_runner`, `spike_scorers`, `enrich_companies`, `poc_test`). These were removed since the LoggerSingleton now handles all log setup and the `_StdlibIntercept` already intercepts stdlib logging calls.
- **SP4: Request middleware query param redaction**: Enhanced middleware to log query params with redaction of sensitive parameter names (`api_key`, `token`, `access_token`, `refresh_token`, `secret`, `password`, `authorization`). The set is defined as a frozen set constant for O(1) lookups.
- **SP4: Pre-existing test failures unchanged**: Same 12 `test_profile_enrichment_service.py` failures (MagicMock/str.join bug) and 1 `test_apify_client.py` import error (`reset_key_cycle`) — both pre-existing and unrelated to SP4 changes. 446 tests pass.

- **SP5: `cli_logged` decorator placed in `dev_tools/cli.py`**: The spec places the decorator alongside existing CLI infrastructure. Command files import it from `dev_tools.cli` — no circular import risk because `cli.py` only imports command modules lazily inside functions.
- **SP5: loguru `bind()` not used for params in decorator**: The spec sketch passed `params=kwargs` as a keyword arg to `logger.info()`, but loguru doesn't accept extra kwargs that way. Instead, key info is included in the log message string directly. Correlation ID is auto-injected by the patcher.
- **SP5: `import time as _time` inside method bodies**: For `controller.py` and `service.py`, `time` is imported locally inside methods to avoid polluting the module namespace and conflicting with existing imports. The decorator in `cli.py` imports `time` at module level since it's the primary consumer.
- **SP5: Enrichment controller uses local timing vars**: `_start` and `_duration_ms` are prefixed with underscore to avoid shadowing any existing names in the method scope.
- **SP5: Pre-existing test failures unchanged**: Same failures as SP4 (test_apify_client import, test_profile_enrichment_service MagicMock, test_best_hop_service SystemExit). 1188 tests pass.

- **SP6: Health check functions use lazy imports inside functions**: `check_db_connection()`, `check_embedding_model()`, `check_api_keys()`, and `get_db_stats()` import `get_config` and `DbSessionManager` locally inside their function bodies (not at module level). This avoids circular imports and allows the functions to be imported without triggering config/DB initialization, which is important for testing and for environments where the DB is not configured.
- **SP6: Repair hooks print guidance instead of running commands directly**: The initial repair hooks (`missing_embeddings`, `missing_affinity`, `stale_enrichment`) print CLI commands for the user to run rather than executing them inline. This is safer for an interactive `--repair` flow — the user confirms then runs the command themselves. Future phases can add hooks that run operations directly.
- **SP6: `clear_repair_hooks()` added for test isolation**: The repair registry is module-level global state. Added `clear_repair_hooks()` to reset it between tests. This is not in the spec but essential for test reliability.
- **SP6: Duplicate hook names silently replaced**: `register_repair_hook()` replaces an existing hook with the same name instead of appending a duplicate. This makes re-registration idempotent (e.g., calling `_register_builtin_repair_hooks()` multiple times is safe).
- **SP6: `get_db_stats()` uses `func.count().select()` pattern**: SQLAlchemy Core-style count queries using `func.count(Entity.id).select()` instead of ORM `session.query().count()`. This matches the project's existing query patterns and works with both ORM and Core sessions.
- **SP6: Tests mock at source module paths (`shared.config.get_config`)**: Since health check functions import `get_config` locally, tests must mock at `shared.config.get_config` (the source) rather than `shared.utilities.health_checks.get_config` (which doesn't exist at module level).

- **SP7: `_StepContext` over `loguru.Logger` subclass for `setup_step` yield**: The context manager yields a lightweight `_StepContext` object with a `.detail()` method rather than the logger itself. This keeps the API simple — callers use the logger separately if they need it, and `.detail()` accumulates messages for the completion log line.
- **SP7: Diagnostic file uses `get_config().log_dir` not hardcoded path**: The diagnostic file path comes from the same config source as the logger, so `LINKEDOUT_LOG_DIR` overrides work for diagnostics too.
- **SP7: `resetDebugCache()` exported from `devLog.ts`**: Added a `resetDebugCache()` function so the cached debug flag can be invalidated when storage changes (e.g., when the user toggles debug mode). Without this, debug mode changes wouldn't take effect until extension reload.
- **SP7: `logApiCall` and `logRateLimit` helper functions added to `log.ts`**: Created typed helper functions rather than requiring callers to manually construct `LogEntry` objects with the new fields. This makes the API self-documenting and ensures consistent field population.

## Phase 04: Constants Externalization
- **SP1: Backend audit found 100 constants across 100 entries**: Categorized into 15 groups. 60 recommended for externalization, 16 already externalized, 15 keep hardcoded, 9 deferred (dev tools or derived). Full audit in `docs/audit/backend-constants-audit.md`.
- **SP1: Extension audit found 32 constants**: 8 flagged as fragile (LinkedIn Voyager internals). 7 recommended for externalization to `config.ts`, 4 already externalized, remainder kept in `constants.ts` or inline. Full audit in `docs/audit/extension-constants-audit.md`.
- **SP1: Voyager decoration IDs documented as fragile but not configurable**: `FullProfileWithEntities-93` and `SearchClusterCollection-186` are LinkedIn internal IDs that can change without notice. Making them user-configurable would be harmful (wrong values break everything silently). They are documented with breakage risk in the extension audit.
- **SP1: Dev tool constants deferred**: Constants in `wikidata_utils.py`, `seed_companies.py`, `enrich_companies.py`, `classify_roles.py`, `download_profile_pics.py`, and `reconcile_stubs.py` are deferred since they're only used in dev/admin tooling, not runtime code.
- **SP1: Intelligence layer has 7 magic numbers**: `search_agent.py` (MAX_ITERATIONS=20, default limit=20), `best_hop_service.py` (MAX_MUTUALS=50), `web_tool.py` (MAX_WEB_SEARCHES=3, timeout=10), `sql_tool.py` (MAX_ROWS=100, TIMEOUT=10000ms), `_sse_helpers.py` (HEARTBEAT=15s). All recommended for externalization.
- **SP1: `CACHE_DAYS=90` in enrichment controller duplicates `settings.enrichment_cache_ttl_days`**: The controller hardcodes `CACHE_DAYS = 90` at line 34 while the same value already exists as `LinkedOutSettings.enrichment_cache_ttl_days`. SP3 should replace the hardcoded value with a config read.
- **SP2: `env_nested_delimiter='__'` added to `SettingsConfigDict`**: Required for nested pydantic model env var overrides (e.g., `LINKEDOUT_SCORING__WEIGHT_CAREER_OVERLAP=0.35`). No existing env vars use `__` in their names so this is safe.
- **SP2: `recency_thresholds` uses `list[list[int | float]]` not `list[tuple[int, float]]`**: Tuples cause issues with YAML deserialization. Using nested lists for compatibility with `yaml.safe_load`.
- **SP2: Recency fallback 0.2 kept hardcoded**: The recency_thresholds config covers the threshold-score pairs `[(12, 1.0), (36, 0.7), (60, 0.4)]`. The final fallback `return 0.2` (for connections beyond 60 months) is not configurable — adding it would extend the plan's scope. It's the implicit "beyond all thresholds" default.
- **SP2: `SOURCE_COUNT_MAP`, `EXTERNAL_SOURCE_TYPES`, `DUNBAR_DEFAULT`, `SENIORITY_DEFAULT_BOOST` kept as module constants**: Not in the sub-phase's config table. `SOURCE_COUNT_MAP` is a data-format constant, `EXTERNAL_SOURCE_TYPES` is a source-type filter, `DUNBAR_DEFAULT` is a tier label, `SENIORITY_DEFAULT_BOOST` is the fallback for unknown seniority levels.
- **SP2: Test file updated to import from `ScoringConfig` defaults**: `test_affinity_scorer.py` previously imported `SENIORITY_BOOST` and `WEIGHT_*` from the scorer module. Updated to import `ScoringConfig` from settings and use `ScoringConfig()` instances for assertions.
- **SP2: `config.yaml.example` created at `backend/config.yaml.example`**: Placed alongside `backend/.env.example` for discoverability. SP7 will consolidate all config.yaml sections from SP2–SP6 into a complete template.
- **SP3: `ACTOR_SCRAPER_MODE` kept as module constant**: Like `APIFY_LINKEDIN_ACTOR_ID`, the scraper mode string is coupled to the Actor's behavior and response format. Making it configurable would be a false promise — changing it would break response parsing. Kept as a non-configurable constant with explanation comment.
- **SP3: Per-request timeout in `poll_run` loop kept hardcoded at 15s**: The inner `requests.get` timeout (15s) during polling is distinct from `run_poll_timeout_seconds` (overall poll timeout) and `key_validation_timeout_seconds`. It's a reasonable per-request guard, not a user-facing knob.
- **SP3: `COST_PER_PROFILE_USD` removed from `apify_client.py` exports**: Controller.py previously imported this constant from `apify_client.py`. Now both files read `enrichment.cost_per_profile_usd` from config directly, eliminating the cross-module import.

- **SP4: Flat Phase 2 fields consolidated into nested models**: `embedding_provider`, `embedding_model`, `llm_provider`, `llm_model`, `search_llm_model`, `summarize_beyond_n_turns`, `prompt_cache_ttl_seconds`, `llm_retry_*`, `llm_timeout_seconds`, `external_api_retry_*`, `external_api_timeout_seconds`, and `rate_limit_llm_rpm` moved from flat `LinkedOutSettings` fields into nested `LLMConfig`, `EmbeddingConfig`, and `ExternalAPIConfig` pydantic models. Env var override via double-underscore nesting (e.g., `LINKEDOUT_LLM__MODEL`).
- **SP4: `embedding_model` default changed from empty string to `text-embedding-3-small`**: Phase 2 used `embedding_model: str = Field(default='')` with the actual default in `EmbeddingClient.__init__`. Now the config is the single source of truth with `EmbeddingConfig.model = 'text-embedding-3-small'`.
- **SP4: `LLMConfig` naming conflict accepted**: `LLMConfig` exists in both `shared.config.settings` (app-level defaults) and `utilities.llm_manager.llm_schemas` (per-client config). They serve different purposes and are in different modules — explicit imports prevent ambiguity.
- **SP4: Retry/timeout module constants replaced with functions**: `LLM_RETRY_CONFIG`, `EXTERNAL_API_RETRY_CONFIG`, `LLM_TIMEOUT_CONFIG`, `EXTERNAL_API_TIMEOUT_CONFIG` module-level constants could not read from config at import time. Replaced with `get_llm_retry_config()` etc. No external consumers existed.
- **SP4: `web_tool.py` model (`gpt-4.1-mini`) not externalized**: This model is for OpenAI's Responses API web search, not the primary LLM or search model. It's a different API and model class. Out of scope for SP4's `LLMConfig`.
- **SP4: `spike_scorers.py` model reference not externalized**: Dev benchmark tool, not production code. Left as-is.
- **SP4: Defensive `or "gpt-5.4-mini"` fallbacks removed from consumers**: `search_agent.py`, `best_hop_service.py`, `why_this_person.py`, `base_agent.py` previously had inline fallback model names. Since config always provides defaults, the fallbacks were removed to enforce single source of truth.
- **SP4: `ExternalAPIConfig` created despite only 2 fields**: The shared-context convention recommends nesting for 3+ fields. `ExternalAPIConfig` has only 2 (`retry_max_attempts`, `timeout_seconds`), but the sub-phase spec explicitly requested it. Followed the spec.

- **SP5: `log_rotation`/`log_retention` placed flat on `LinkedOutSettings`**: Only 2 fields, so per shared-context convention (nest only when 3+ related fields), they stay flat rather than creating a `LoggingConfig` model. They already have env var overrides via `LINKEDOUT_LOG_ROTATION` / `LINKEDOUT_LOG_RETENTION`.
- **SP5: Phase 3 already corrected rotation/retention defaults**: `logger.py` was already reading from env vars with correct defaults (50 MB / 30 days) after Phase 3. SP5 changed it to read from config fields instead, which still respects env var overrides through pydantic-settings resolution.
- **SP5: `default_page_size` placed flat on `LinkedOutSettings`**: Single standalone field, not nested. Env var: `LINKEDOUT_DEFAULT_PAGE_SIZE`.
- **SP5: Entity ID prefix comment on `BaseEntity.id_prefix` only**: Rather than adding comments to all 36 entity files, the explanation is on the `id_prefix` class variable in `BaseEntity` where the pattern is established.
- **SP6: `mutualPageSize` NOT added to `ExtensionConfig`**: The sub-phase plan listed `mutualPageSize` as a config field, but the SP1 audit specifically recommends keeping `PAGE_SIZE` in `constants.ts` because it "must match LinkedIn's API." Making it configurable would be a false promise — wrong values silently break pagination. Moved to `constants.ts` as `MUTUAL_PAGE_SIZE` instead.
- **SP6: Search decoration ID moved to `constants.ts`**: `DECORATION_ID` was defined locally in `extractor.ts`. Moved to `constants.ts` as `VOYAGER_SEARCH_DECORATION_ID` alongside `VOYAGER_DECORATION_ID` so all fragile LinkedIn internals are co-located and documented together.
- **SP6: Added `recentActivityLimit`, `mutualFirstPageDelayBaseMs`, `mutualFirstPageDelayRangeMs` to config**: The audit identified these as configurable values not in the plan's interface. Added them to keep parity with the audit's recommendations (7 values to externalize).
- **SP7: `config.yaml` template embedded in `docs/configuration.md`**: Rather than creating a separate `config.yaml.example` file, the complete template is in Section 16 of the configuration reference. This avoids having two files to keep in sync (the reference doc and the template). The `backend/config.yaml.example` from SP2 remains for discoverability alongside `.env.example`.
- **SP7: Grep verification exceptions documented**: The exit criteria greps (`gpt-5|gpt-4|text-embedding`, `localhost:8001`, `= 1536`) return non-zero results due to documented exceptions from prior sub-phases: `web_tool.py` (SP4: different API, intentionally not externalized), `llm_schemas.py` (docstring examples), `spike_scorers.py` (dev tool, deferred), `fe_setup.py` (dev setup helper), `settings.py` (IS the config file — grep spec uses `config.py` but the file was renamed to `settings.py` in SP2).
- **SP7: `.env.example` uses double-underscore notation for nested config**: Added section headers noting `(nested — use double underscore)` to make the nesting convention visible to users who only read `.env.example` without consulting the full docs.

## Phase 05: Embedding Abstraction
- **SP1: OPENAI_API_KEY check changed from hard error to warning**: The Phase 2 model_validator raised `ValueError` when `embedding.provider == 'openai'` and `openai_api_key is None`. Changed to `logging.warning()` per sub-phase spec — the key is only needed at embed time, not at startup. This prevents the app from crashing on startup for users who haven't yet configured OpenAI but want to use local embeddings or haven't reached the embedding step.
- **SP1: `EmbeddingConfig.provider` field validator added**: Validates that `embedding.provider` must be `'openai'` or `'local'` — no `'auto'` mode. This is a field_validator on the nested `EmbeddingConfig` model, not the top-level `LinkedOutSettings`, so it fires during model construction.
- **SP1: `build_embedding_text()` extracted as standalone function**: Extracted from `EmbeddingClient.build_embedding_text()` static method into `embedding_provider.py`. `EmbeddingClient.build_embedding_text()` now delegates to the standalone function for backward compatibility.
- **SP1: Pre-existing test failures unchanged**: Same 2 failures from Phase 4: `test_apify_client.py` (imports `reset_key_cycle` removed in Phase 2), `test_health_checks.py::test_openai_with_key_returns_pass` (mocks `embedding_provider` flat but code uses `embedding.provider` nested — pre-existing mock/code mismatch), `test_profile_enrichment_service.py` (MagicMock in str.join). 476 tests pass.

- **SP2a: No changes to `embedding_client.py` needed**: SP1 already updated `build_embedding_text()` to delegate to the standalone function in `embedding_provider.py`. The deliverable #2 (update embedding_client.py) was already complete.
- **SP2a: `RuntimeError` for missing API key, not `ValueError`**: The provider raises `RuntimeError` with actionable setup instructions when `OPENAI_API_KEY` is not configured. This is a runtime environment issue, not a validation error — `RuntimeError` is more semantically correct and distinguishable from pydantic `ValueError`.
- **SP2a: Pre-existing test failures unchanged**: Same failures as SP1: `test_apify_client.py` (import error), `test_health_checks.py::test_openai_with_key_returns_pass` (mock mismatch), `test_profile_enrichment_service.py` (MagicMock in str.join). 311 unit tests pass (excluding the 1 import error file).

- **SP2c: Manual Alembic migration instead of autogenerate**: Wrote the migration manually with raw SQL (`op.execute()`) matching the pattern in `c1e2f3a4b5d6_embedding_text_to_vector.py`, rather than using `alembic revision --autogenerate`. This gives precise control over the column rename (which autogenerate can't detect — it would drop + recreate, losing data) and HNSW index operations.
- **SP2c: Downgrade recreates the original HNSW index**: The downgrade path not only drops the new indexes and renames back, but also recreates `ix_cp_embedding_hnsw` to restore the exact state from the prior migration.
- **SP2c: `_get_embedding_column()` reads from `backend_config.embedding.provider`**: Uses the nested pydantic-settings config path (`backend_config.embedding.provider`) rather than `getattr(backend_config, 'LINKEDOUT_EMBEDDING_PROVIDER', ...)` since the config system was updated in Phase 2/SP1 to use `EmbeddingConfig` nested model.
- **SP2c: Expected test failure in `affinity_scorer.py`**: The `profile.embedding` attribute rename causes `test_career_overlap_with_shared_companies` to fail. Per sub-phase spec notes, this is intentional — sp3 (Factory + Update Callers) will update all callers including `affinity_scorer.py`. 316 tests pass, 3 fail (1 new expected + 2 pre-existing).

- **SP2d: `mark_failed(error)` parameter kept but unused in body**: The spec defines `mark_failed(self, error: str)` which only sets `status` and `updated_at`. The `error` param is kept in the signature so callers self-document the failure reason at the call site, but the value is not stored in the progress file — the caller logs it separately. Added `# noqa: ARG002` to silence linters.
- **SP2d: `get_progress_path()` reads `backend_config.data_dir` directly**: Uses the already-expanded `data_dir` from settings (which handles `~/` expansion in `_expand_paths_and_compute`), then wraps it in `os.path.expanduser()` defensively. No need for `getattr()` fallback since `data_dir` always has a default.
- **SP2d: Pre-existing test failures unchanged**: Same 2 failures: `test_health_checks.py::test_openai_with_key_returns_pass` (mock mismatch), `test_affinity_scorer.py::test_career_overlap_with_shared_companies` (expected — profile.embedding renamed in sp2c). 287 tests pass + 12 new embedding_progress tests pass.

- **SP3: `get_embedding_column_name()` placed in `embedding_factory.py`**: The sub-phase spec suggested placing the helper in the service or as a standalone utility. Placed it in `embedding_factory.py` alongside the factory function since it's a provider-to-column mapping concern — all callers import from the same module.
- **SP3: `search_controller.py` updated for column-aware queries**: The sub-phase spec listed 4 caller files to update, but verification grep found `search_controller.py` also had raw SQL referencing `cp.embedding`. Updated its `_SIMILAR_SQL` to a function `_get_similar_sql(col)` and the connection lookup query to use the active embedding column.
- **SP3: `post_enrichment.py` and `tasks.py` updated as discovered callers**: These files created `EmbeddingClient()` and passed it to `ProfileEnrichmentService`. Updated to use `get_embedding_provider()` and `EmbeddingProvider` types.
- **SP3: `crawled_profile_controller.py` updated**: The controller's `_get_enrichment_service()` dependency created `EmbeddingClient()`. Updated to use `get_embedding_provider()`.
- **SP3: Pre-existing test mock bug fixed in `test_profile_enrichment_service.py`**: The test's `_patch_repo_get` only patched the repository, but `enrich()` queries via `session.query()`. Added session query chain stub to return the mock profile. This was a pre-existing bug documented in Phase 2/SP7 and Phase 3/SP1 logs.
- **SP3: Pre-existing test failures unchanged**: `test_apify_client.py` (import error from Phase 2), `test_health_checks.py::test_openai_with_key_returns_pass` (mock/code mismatch). 558 unit tests pass.

- **SP4: `_LazyLinkedOutGroup` pattern for circular import avoidance**: `generate_embeddings.py` imports `cli_logged` from `cli.py`, and `cli.py` needed to import `embed_command` from `generate_embeddings.py`. Used a lazy Click Group subclass (`_LazyLinkedOutGroup`) that defers the import to `list_commands()`/`get_command()` — avoids circular import at module load time.
- **SP4: `--resume` flag removed from CLI surface**: The spec showed `--resume` as an option (default: true). Since resume-by-default is the behavior and `--force` is the override, the explicit `--resume` flag added no value. Removed to keep the interface simpler per SJ's simplicity bias.
- **SP4: `--batch` kept as hidden-ish OpenAI-specific flag**: Per spec notes, `--batch` is kept for power users who want to use the cheaper OpenAI Batch API. `--resume-batch-id` and `--fail-and-push` removed as too complex for OSS v1.
- **SP4: Pre-existing test failures fixed**: Fixed 4 `test_health_checks.py` failures (mock used flat `embedding_provider` instead of nested `embedding.provider` + entity used removed `embedding` column instead of `embedding_openai`/`embedding_nomic`). Fixed 2 `test_settings.py` failures (test expected `ValidationError` for missing OpenAI key but code now warns, and test used `s.embedding_provider` flat attribute instead of `s.embedding.provider`). Fixed 5 `test_people_like_x.py` failures (tests didn't mock `get_embedding_provider` which now tries to instantiate real OpenAI provider). All pre-existing from sp2c/sp3 refactoring. 1385 tests pass (excluding `test_apify_client.py` import error from Phase 2).
- **SP4: `backward-compatible main = embed_command` alias**: Kept `main` as an alias for `embed_command` so the existing `rcv2 db generate-embeddings` CLI path continues to work during transition.
- **SP4: Separate JSON report + OperationReport**: The embed command writes both a detailed `embed-{provider}-YYYYMMDD-HHMMSS.json` report (with provider-specific fields like dimension) and uses the `OperationReport` dataclass. The JSON report is the primary artifact; `OperationReport` ensures the CLI output pattern is consistent.

## Phase 06: Code Cleanup
- **SP2: `common/` module kept entirely**: Grep analysis showed `common/` is heavily used by `organization/`, `linkedout/enrichment_pipeline/`, `linkedout/crawled_profile/`, and many other modules. NOT project_mgmt-specific — it contains base classes (BaseEntity, BaseService, BaseRepository, CRUDRouterFactory) and the agent_run CRUD stack.
- **SP2: `agent_run_router` kept in main.py**: The `agent_run` infrastructure in `common/` is generic agent execution tracking, not project_mgmt-specific. Used by `dev_tools/`, `shared/test_utils/seeders/`, and entity factories. Only the `project_mgmt.agents` import (which registered the TASK_TRIAGE agent) was removed.
- **SP2: project_mgmt references cleaned from 10 files outside project_mgmt/**: `main.py`, `db_session_manager.py`, `entity_factories.py`, `base_seeder.py`, `seed_config.py`, `validate_orm.py`, `verify_seed.py`, `fixed_data.py`, `run_agent.py`, `cli.py`, `base_entity.py` (TableNames enum), `conftest.py` (integration tests).
- **SP2: FastAPI app metadata updated**: Title changed from "Sample Backend API" to "LinkedOut API", description updated to remove project management references.
- **SP3: Manual retry instead of tenacity**: `tenacity` is not in `requirements.txt`. Implemented manual retry loop (3 attempts, 1s/2s/4s backoff) in `_run_enrichment()` method on the controller. Simpler and avoids adding a dependency.
- **SP3: Enrichment logic inlined in controller, not extracted to service.py**: The sub-phase spec offered "service.py or inline in controller" as options. Inlined in the controller's `_EnrichmentTriggerService` class since it's the only caller and the logic is straightforward (get key → call Apify → delegate to PostEnrichmentService). No need for a separate file.
- **SP3: `EnrichmentController` class does not exist**: The verification command references `from ... import EnrichmentController` but the actual export is `enrichment_pipeline_router`. The sub-phase spec's check #6 is technically wrong — verified with the actual router import.
- **SP4: `langfuse_enabled` (lowercase) used instead of `LANGFUSE_ENABLED`**: The spec's guard code sketch used `getattr(backend_config, 'LANGFUSE_ENABLED', False)` but the actual pydantic-settings field is `langfuse_enabled` (lowercase). Guard uses `backend_config.langfuse_enabled`.
- **SP4: `propagate_attributes` added to guard**: Not in the original guard spec sketch, but `best_hop_controller.py` imports `propagate_attributes` from langfuse alongside `get_client`. Added as a no-op context manager to the guard.
- **SP4: `llm_client.py` langfuse imports made lazy**: The file already had its own `enable_tracing` conditional logic. Module-level `from langfuse.langchain import CallbackHandler` moved inside `_create_langfuse_handler()` (only executes when tracing is enabled). `from langfuse import get_client` replaced with guard import.
- **SP4: `prompt_manager/langfuse_store.py` NOT modified**: Per SJ decision Q2 and shared context, `utilities/prompt_manager/` is kept as-is. This file retains its direct `from langfuse import Langfuse` import.
- **SP4: Verification check #3 adapted**: The spec's check imports `VectorTool` class which doesn't exist — the module exports functions (`search_profiles`). Verified with `from linkedout.intelligence.tools.vector_tool import search_profiles` instead.
- **SP5: `download_profile_pics.py` hardcoded path replaced with `get_config().data_dir / 'images'`**: The sub-phase spec mentioned `benchmark/runner.py:214` as having a hardcoded DB config path, but inspection showed it already uses `get_config().database_url`. The actual hardcoded path was in `download_profile_pics.py` (`/home/sridherj/workspace/linkedout-images`). Replaced with `Path(get_config().data_dir) / 'images'` which defaults to `~/linkedout-data/images`.
- **SP5: `dev_tools/cli.py:445` hardcoded `~/workspace/linkedout-fe` left for sp6**: The `fe` command is being retired in sp6 (CLI refactor). The entire `dev_tools/cli.py` is deleted in sp6, so no fix needed here.
- **SP5: `shared/test_endpoints/` directory deleted entirely**: Only contained `sse_router.py` and `__init__.py`. Removed the import and `include_router` call from `main.py`.
- **SP5: Private-repo scan clean**: `sk-` grep matches are all placeholder examples in help text (e.g., `sk-...` in config error messages). No `PRIVATE` markers or `TODO.*private` comments found.
- **SP6: `dev_tools/db/fixed_data.py` kept in place**: `SYSTEM_USER_ID` and other fixed data constants are used by new `linkedout/commands/` modules and by `shared/test_utils/`, `linkedout/intelligence/tools/`, etc. Keeping in `dev_tools/db/` avoids a large import-churn refactor. The `dev_tools/` directory retains `company_utils.py`, `reconcile_stubs.py`, `diagnostics.py`, and other dev-only scripts.
- **SP6: `cli_logged` decorator moved to `linkedout/cli_helpers.py`**: Extracted from the deleted `dev_tools/cli.py` into a shared helper module. All new commands import from `linkedout.cli_helpers` instead.
- **SP6: `config` implemented as Click sub-group, not plain command**: `docs/decision/cli-surface.md` specifies `config show` and `config path` as sub-commands. Implemented as `@click.group('config')` with two sub-commands, registered on the main CLI group.
- **SP6: `start-backend` default host changed to 127.0.0.1**: The old `dev start` bound to `0.0.0.0`. Changed to `127.0.0.1` per `docs/decision/cli-surface.md` for security (localhost-only by default).
- **SP6: `reset-db` simplified from 3 modes to 2**: Old command had truncate/drop/complete-reset modes. New command defaults to truncate, `--full` does drop+migrate. No more `--seed` flag (use `linkedout import-seed` separately). No more interactive menu.
- **SP6: Legacy entry points fully removed**: All 20+ entry points from `pyproject.toml` (`rcv2`, `pm`, `dev`, `be`, `fe`, `fe-setup`, `run-all-agents`, `reset-db`, `seed-db`, `verify-seed`, `validate-orm`, `precommit-tests`, `eval-tests`, etc.) replaced by single `linkedout = "linkedout.cli:cli"`.
- **SP6: `rcv2` references cleaned from dev_tools/**: Updated string references in `diagnostics.py`, `fe_setup.py`, and `db/load_fixtures.py` to use `linkedout` namespace. Zero `rcv2` matches in `backend/**/*.{py,toml}`.
- **SP7: `agent_run` table included in baseline**: Grep analysis confirmed `agent_run` is used by `search_agent.py`, `agent_executor_service.py`, `agent_run_service.py`, `agent_run_controller.py`, `agent_run_repository.py`, and test utils — NOT project_mgmt-specific. Included in baseline.
- **SP7: Pipeline infra tables excluded from baseline**: `news_article`, `news_company_mention`, `pipeline_state`, `pipeline_failed_item`, `raw_feed_item`, `extracted_company`, `discovery_signal` — these are DB-only tables with no ORM entities. They are excluded from Alembic autogenerate via `_EXCLUDE_TABLES` in `env.py` and managed by separate infrastructure. Not included in the baseline migration.
- **SP7: RLS policies use optimized EXISTS form**: The baseline uses the EXISTS-subquery RLS policy pattern (from the later optimization migration `f8af651f8b3b`), not the original IN-subquery pattern. This avoids the sequential scan performance issue with large connection tables.
- **SP7: Embedding columns created via raw SQL**: `embedding_openai vector(1536)` and `embedding_nomic vector(768)` are added via `op.execute()` ALTER TABLE statements since Alembic's `op.create_table()` doesn't natively handle pgvector `vector()` types without extra setup.
- **SP7: Affinity indexes use DESC NULLS LAST**: The baseline creates `ix_conn_app_user_affinity` and `ix_conn_tenant_affinity` with `DESC NULLS LAST` sort order (from the P1 index optimization migration), not the default ASC sort.
- **SP7: `search_turn` entity import added to env.py**: The original `env.py` was missing `import linkedout.search_session.entities.search_turn_entity`. Added so Alembic's autogenerate can detect the table.
- **SP7: `generate_nanoid` SQL function not included**: This function was only used by pipeline infra tables (which are excluded). ORM entities generate IDs in Python via `Nanoid.make_nanoid_with_prefix()`.
- **SP7: No live DB validation**: OSS repo has no `.env` with real credentials. Migration validated via `alembic heads` (single head confirmed). Full DB testing deferred to sp9 (Test Suite Green).
- **SP8: `plotly`, `pandas`, `dash`, `dash-bootstrap-components` removed entirely**: Not imported anywhere in `backend/src/` or `backend/tests/`. Leftover from a removed feature.
- **SP8: `scipy` moved to dev deps**: Only imported in `dev_tools/benchmark/spike_scorers.py` (benchmarking tool). Not production code.
- **SP8: `tenacity` added to requirements.txt**: Directly imported in `shared/infra/reliability/retry_policy.py` but was missing from requirements.txt (only installed transitively via langchain). Made explicit.
- **SP8: `httpx` kept in production deps**: Used in `dev_tools/download_profile_pics.py`, `dev_tools/wikidata_utils.py`, `dev_tools/enrich_companies.py` (production CLI utilities), not just tests.
- **SP8: `pydantic-core` kept as explicit dep**: Transitive dependency of `pydantic`, but kept to maintain existing pinning strategy per sub-phase guidelines.
- **SP9: `conftest.py` project_mgmt entity imports removed**: Root conftest imported 4 `project_mgmt.*` entities for SQLAlchemy model registration. Removed since the domain was deleted in sp2.
- **SP9: `tests/integration/project_mgmt/` directory deleted**: Entire integration test directory for project_mgmt was missed during sp2. Contained tests for label, priority, project, task controllers. Deleted.
- **SP9: Embed command test imports updated to `linkedout.commands.embed`**: `generate_embeddings.py` was moved to `linkedout/commands/embed.py` in sp6. Updated imports and all `patch()` targets in `tests/unit/cli/test_embed_command.py` and `tests/integration/test_embed_command.py`.
- **SP9: `CallbackHandler` patch targets updated to `langfuse.langchain.CallbackHandler`**: sp4 moved the `CallbackHandler` import from module-level to lazy (inside `_create_langfuse_handler()`). Tests that patched `utilities.llm_manager.llm_client.CallbackHandler` failed. Updated to patch at `langfuse.langchain.CallbackHandler`.
- **SP9: Apify key rotation tests rewritten**: `reset_key_cycle` was removed when key rotation was simplified to single-key-from-config. Rewrote `TestGetPlatformApifyKey` to test the new `get_config().apify_api_key` pattern.
- **SP9: Pre-existing lint/type errors documented as baseline**: 79 ruff errors and 178 pyright errors are pre-existing. Zero new errors introduced by Phase 6. 5 pyright errors in `linkedout/commands/embed.py` and `import_contacts.py` are pre-existing patterns that moved with the code from `dev_tools/`.
- **SP8: `pyrefly` moved to dev deps**: Meta's Python type checker. Not imported anywhere in source code — dev-only tool.

## Phase 07: Seed Data

### SP2: Seed Export Curation Script

- **PII column mapping**: CrawledProfile entity has no `email` or `phone` columns (spec names were illustrative). Actual PII-bearing fields NULLed: `notes` (BaseEntity internal notes), `raw_profile` (raw JSON that may contain PII), `source_app_user_id` (FK to internal app_user table not in seed data).
- **Full tier web traffic filter**: No `web_traffic_rank` column exists on the company entity. Used `size_tier IS NOT NULL` as the closest proxy, combined with `estimated_employee_count > 0` and funding round existence.
- **Embedding columns excluded entirely**: pgvector `Vector(N)` types have no SQLite equivalent. Columns `embedding_openai`, `embedding_nomic`, `embedding_model`, `embedding_dim`, `embedding_updated_at`, and `search_vector` are excluded from the export (not just NULLed). Aligns with decision that seed data does not include embeddings.
- **Subquery-based tier filtering**: Instead of materializing company/profile ID sets in Python (which would require large `IN` parameter lists for 100K+ IDs), tier filtering uses PostgreSQL subqueries embedded in each table's export query. PostgreSQL optimizes these efficiently.
- **Idempotency via deterministic timestamps**: `_metadata.created_at` uses `MAX(updated_at)` from the tier's qualifying companies rather than `datetime.now()`. Combined with `PRAGMA journal_mode=DELETE`, `ORDER BY id`, and `VACUUM`, this ensures byte-identical SQLite output for unchanged source data.
- **ARRAY columns serialized as JSON**: PostgreSQL `ARRAY(Text)` columns (e.g., `enrichment_sources`, `lead_investors`, `all_investors`) are serialized as JSON strings in SQLite for portability.

### SP3: Download Seed Command

- **Shared checksum utility**: Extracted `compute_sha256` from `dev_tools/seed_export.py` into `shared/utils/checksum.py` with an additional `verify_checksum` helper. Both `seed_export.py` and `download_seed.py` now import from the shared location, avoiding duplication.
- **File path convention**: The sub-phase spec specified `backend/src/linkedout/cli/commands/download_seed.py` but the actual codebase uses `backend/src/linkedout/commands/download_seed.py` (no nested `cli/commands/` directory). Followed the existing codebase convention.
- **Temp file download pattern**: Downloads write to a `.tmp` file first, then rename on success. If the download fails partway through, the partial file is cleaned up rather than leaving a corrupt file at the destination path.
- **Dual report output**: Generates both an `OperationReport` (via the shared infrastructure) for consistency with other CLI commands, and a detailed download-specific JSON report with version, tier, sha256, source URL, and duration as specified in the sub-phase.
- **GitHub token support**: All HTTP requests to GitHub (API and downloads) include `Authorization: token` header if `GITHUB_TOKEN` env var is set, enabling use in CI and avoiding rate limits.

### SP4: Import Seed Command

- **File path convention**: Followed SP3 precedent — placed at `backend/src/linkedout/commands/import_seed.py` (not `cli/commands/` as spec stated). The sub-phase spec was illustrative; actual codebase uses `linkedout/commands/`.
- **Raw SQL upsert over ORM**: Used Option B (raw SQL with `ON CONFLICT (id) DO UPDATE`) consistently for all tables. The `RETURNING (xmax = 0) AS inserted` clause distinguishes inserts from updates, and the `WHERE ... IS DISTINCT FROM ...` clause skips rows where all columns match (idempotency). This is simpler than mixing ORM and SQL approaches.
- **Row-by-row within batches**: Although batching is used for progress reporting (1000-row chunks), each row executes individually within the batch to track per-row insert/update/skip outcomes via the RETURNING clause. For the target dataset sizes (~100K rows), this completes well within the 5-minute target.
- **Dual report output**: Follows SP3 pattern — generates both an `OperationReport` (shared infrastructure) and a detailed import-specific JSON report with per-table counts.
- **BOOL_COLUMNS only for tables that have booleans**: Instead of listing empty sets for every table, BOOL_COLUMNS only contains tables with actual boolean columns, using `.get(table_name, ())` for lookup.
- **Dry-run reads DB for accurate counts**: Dry-run mode reads existing PostgreSQL IDs to distinguish "would insert" from "would skip/update", rather than just reporting raw SQLite row counts. This gives users actionable information.
- **Single WRITE session for all tables**: All 10 tables are imported within a single `DbSessionType.WRITE` session/transaction, so either all tables import or none do (atomic).

### SP5: GitHub Release Publishing

- **Inline release section in README**: Added a "Publishing a Seed Data Release" section to `seed-data/README.md` with copy-pasteable `gh` commands rather than a separate doc. The existing "For Maintainers" section now cross-references it.
- **Optional GH Actions workflow**: Created `.github/workflows/release-seed-data.yml` as a `workflow_dispatch` manual trigger. Marked as placeholder because it cannot access the production database — seed files must be generated locally. The manual `gh release create` process is the primary path.
- **Release tag convention**: Documented `seed-v{semver}` format (e.g., `seed-v0.1.0`) to keep seed releases separate from code releases.

### SP6: Integration Testing

- **Fixture generation script**: Created `backend/tests/fixtures/generate_test_seed.py` as a standalone Python script that generates `test-seed-core.sqlite`. The script uses the same column schemas as the seed export (including PII-NULLed columns like `notes`, `raw_profile`, `source_app_user_id`). Row counts: 10 companies, 15 aliases, 10 role aliases, 8 funding rounds, 5 startup tracking, 12 growth signals, 20 profiles, 40 experiences, 25 educations, 30 skills (175 total).
- **Test file paths follow existing convention**: Tests placed at `backend/tests/unit/cli/` and `backend/tests/integration/cli/` matching the existing `test_embed_command.py` pattern, not `backend/tests/unit/cli/commands/` as the sub-phase spec illustratively suggested.
- **Integration tests use existing conftest fixtures**: Integration tests rely on `integration_db_session` from the existing `backend/tests/integration/conftest.py`, which manages test schema creation/teardown and PostgreSQL connection validation with graceful skip.
- **Integration tests skip without PostgreSQL**: Tests are marked `@pytest.mark.integration` and use the existing conftest pattern that calls `pytest.skip()` when DATABASE_URL is not configured or PostgreSQL is unreachable — safe for CI environments without a database.

## Phase 08: Skill System
- **SP1: Regex-based template engine over Jinja2**: Used lightweight regex-based approach for `{{VAR}}` and `{{#if VAR == "val"}}` syntax per spec. No external template dependencies. Supports `==` and `!=` operators only — no nested conditionals, no loops.
- **SP1: Version read from pyproject.toml**: `get_global_context()` reads version from `backend/pyproject.toml` via regex, falls back to `VERSION` file at repo root, then `0.0.0`. No TOML parser dependency needed.
- **SP1: PYTHONPATH=. required for skills imports**: Since `skills/` is at repo root (not inside `backend/`), tests and CLI usage require `PYTHONPATH=.` (or repo root on sys.path). Tests run with `PYTHONPATH=. uv run pytest tests/skills/`.
- **SP1: Tests in `tests/skills/` at repo root**: Per spec, skill infrastructure tests are separate from `backend/tests/` since they test cross-cutting skill tooling, not backend code.
- **SP2: `build_schema_context()` found at `linkedout.intelligence.schema_context`**: The sub-phase doc referenced `backend/src/utilities/llm_manager/schema_context.py` but the actual module is `backend/src/linkedout/intelligence/schema_context.py`. Script path updated accordingly.
- **SP2: `DB_SCHEMA_SNIPPET` reads file at render time**: Added `_read_schema_snippet()` to `skills/lib/config.py` that reads `skills/linkedout/schema-reference.md` at template render time and strips the auto-generated header comment. This keeps the template engine simple (no file-include syntax).
- **SP2: Schema reference auto-generated without DB**: `build_schema_context()` introspects SQLAlchemy entity `__table__` metadata which is defined at import time — no live DB connection needed. The `bin/generate-schema-ref` script works standalone.
- **SP2: RLS note in schema-reference.md says no user filtering needed**: The business rules from `schema_context.py` state RLS auto-scopes queries. However, the sub-phase spec says to include `WHERE tenant_id = $TENANT_ID ...` filters. Kept the upstream business rules as-is since they're authoritative (generated from source code), and RLS is the correct pattern for the OSS single-user setup.
- **SP3: Stubs kept minimal and actionable**: Each stub is 30-55 lines of template content with working CLI commands and clear manual steps. Stubs explain what they do and provide useful guidance rather than just "coming soon" messages.
- **SP3: No `linkedout-extension-setup` stub created**: Per decision #6, extension setup skill is Phase 12 work after UX design gate. Exactly 5 stubs created.
- **SP3: `linkedout-setup` uses `Write` tool**: The setup skill includes `Write` in its tools list because it needs to create `agent-context.env`. Other stubs use only `Bash` + `Read` since they're read-only operations.
- **SP5: `bin/generate-skills` created in sp5 (not sp4)**: The generation script didn't exist when sp5 started. Created it as part of sp5 since routing generation depends on it. The script handles both skill templates and routing templates in one pass.
- **SP5: `output_dir` field added to claude.yaml**: The host name is `claude` but the output directory convention is `claude-code`. Added `output_dir: claude-code` to `skills/hosts/claude.yaml` to handle this mapping. Codex and Copilot host names match their directory names so they don't need this field.
- **SP5: Routing symlink placement**: Routing files (CLAUDE.md, AGENTS.md, COPILOT.md) are symlinked one level up from the skill install path (e.g., `~/.claude/skills/CLAUDE.md` not `~/.claude/skills/linkedout/CLAUDE.md`). This matches the convention where routing files sit alongside skill directories, not inside them.
- **SP5: `dev-teardown` checks all hosts, not just detected**: Teardown iterates all configured hosts (not just detected ones) to clean up symlinks even if the host directory was removed after initial setup.

## Phase 09: Setup Flow
- **UX design docs generated upfront**: All 3 design gate docs (P09, P10, P12) were generated before implementation. SJ reviewed and approved with agent-suggested improvements integrated.
- **SP3: `set_db_password` tries peer auth first, falls back to sudo**: The database password is set via `ALTER USER` — first tries connecting as the `linkedout` user (peer auth), then falls back to `sudo -u postgres psql` if that fails. This handles both fresh installs (where peer auth isn't configured yet) and re-runs.
- **SP3: `_find_backend_dir` walks up from source file**: Alembic needs to run from the `backend/` directory where `alembic.ini` lives. Rather than hardcoding a relative path, `_find_backend_dir()` walks up the file's parent chain looking for `alembic.ini`.
- **SP3: Reuses `shared.config.agent_context.generate_agent_context`**: Instead of duplicating agent-context.env generation, `database.py` imports and delegates to the existing Phase 2 utility. A temporary `LinkedOutSettings` instance is created with the generated database_url.
- **SP3: Config.yaml gets chmod 600**: Since config.yaml contains the database password (per env-config-design.md: "DB password in config.yaml, not secrets.yaml"), the file is created with owner-only read/write permissions.
- **SP3: system-setup.sh macOS uses Homebrew without sudo**: On macOS, `createuser` and `createdb` run without `sudo -u postgres` since Homebrew PostgreSQL runs as the current user.
- **SP4: Owner profile identified by `data_source='setup'`**: Rather than adding an `is_owner` column to `crawled_profile`, the owner profile is identified by `data_source='setup'`. This avoids schema changes — the existing `data_source` column serves double duty.
- **SP4: SQLAlchemy raw SQL for profile creation**: `user_profile.py` uses raw SQL (`text()`) instead of the ORM entity to avoid importing the full entity graph (which triggers DB session manager initialization). This keeps setup modules lightweight and testable.
- **SP4: `getpass` for API key input**: Both OpenAI and Apify keys are collected via `getpass.getpass()` to suppress terminal echo. This matches the security requirement that API keys never appear in terminal output.
- **SP4: Secrets.yaml None values written as comments**: When a key is `None` (e.g., user skipped Apify), it's written as `# apify_api_key:` rather than omitted, so users can see what keys are available and uncomment later.
- **SP4: `update_config_yaml` uses regex replacement**: To preserve comments and formatting in existing config.yaml, the update function uses regex to find and replace specific key lines rather than round-tripping through YAML parse/dump (which strips comments).
- **SP5: CLI invoked via `sys.executable -m linkedout.commands`**: All three data import modules (csv_import, contacts_import, seed_data) invoke CLI commands via `subprocess.run([sys.executable, "-m", "linkedout.commands", ...])` rather than direct function calls. This avoids circular imports with the full app stack and keeps setup modules as thin wrappers around existing CLI commands (as specified in the sub-phase notes).
- **SP5: `data_dir` parameter accepted but unused in `setup_contacts_import`**: The `data_dir` parameter is kept in the function signature for consistency with all other `setup_*` orchestration functions (csv_import, seed_data, user_profile, api_keys all accept it). Contacts import doesn't need it since it doesn't copy files to the data directory.
- **SP5: Seed checksum uses GNU coreutils format**: `verify_seed_checksum` parses `CHECKSUM` files in the `<sha256hex>  <filename>` format (two spaces between hash and filename), matching GNU coreutils `sha256sum` output. This is the standard format for GitHub Release checksums.

- **SP6: Embedding/affinity modules are subprocess wrappers**: Both `embeddings.py` and `affinity.py` invoke existing CLI commands (`linkedout embed`, `linkedout compute-affinity`) via subprocess rather than calling internal functions directly. This matches the SP5 pattern and avoids circular imports with the full app stack.
- **SP6: `count_profiles_needing_embeddings` uses `embed --dry-run`**: Rather than querying the database directly (which would require importing the session manager and provider factory), the function delegates to `linkedout embed --dry-run` and parses the output. Keeps setup modules as thin orchestration wrappers.
- **SP6: `check_user_profile_exists` uses `compute-affinity --dry-run`**: Similarly delegates to the CLI dry-run command to check if any users exist, avoiding direct DB queries.
- **SP6: `db_url` and `data_dir` params accepted for interface consistency**: Both `setup_embeddings(data_dir, db_url)` and `setup_affinity(data_dir, db_url)` accept these parameters (matching all other `setup_*` functions) even though the subprocess approach reads config internally. Marked with `noqa: ARG001`.
- **SP6: Cost estimate uses $0.01 per 1K profiles (conservative)**: OpenAI text-embedding-3-small with Batch API actually costs ~$0.002 per 1K profiles, but we advertise ~$0.01 as a conservative upper bound matching the UX design doc.

- **SP7: Readiness report uses `ReadinessReport` dataclass, not `OperationReport`**: The readiness report has a fundamentally different structure (counts, coverage, config, skills, gaps, next_steps) than operation reports (succeeded/skipped/failed). A dedicated `ReadinessReport` dataclass avoids shoehorning into `OperationReport`. The JSON schema matches the sub-phase spec exactly.
- **SP7: DB counts via `linkedout diagnostics --json` subprocess**: Rather than importing the session manager and querying directly (which would require the full app stack), `readiness.py` delegates to `linkedout diagnostics --json` and parses the JSON output. Consistent with SP5/SP6 subprocess-wrapper pattern.
- **SP7: `db_url` param accepted but unused (noqa: ARG001)**: `collect_readiness_data(db_url, data_dir)` accepts `db_url` for interface consistency with all other setup modules, even though the subprocess reads config internally.
- **SP7: Auto-repair uses lambda factories for RepairAction construction**: Each gap type maps to a lambda in `_REPAIR_MAP` that constructs a `RepairAction` with gap-specific counts and descriptions. This keeps the mapping declarative and avoids a large if/elif chain.
- **SP7: Stale embeddings default_accept is False**: Missing embeddings/affinity default to `[Y/n]` (cheap, quick fixes), but stale embeddings (provider mismatch) defaults to `[y/N]` since re-embedding is expensive and optional. Matches UX design doc Prompt 14c.
- **SP7: Skill install uses `shutil.copy2` not symlinks**: Copies generated skill files rather than symlinking. This ensures skills work even if the repo is moved or deleted, and matches the UX design doc's "Install to ~/.claude/skills/linkedout/" wording.
- **SP7: Platform detection is conservative (directory must exist)**: `detect_platforms()` only claims a platform is installed if its config directory (e.g., `~/.claude/`) exists as a real directory. No attempt to detect via process lists or registry entries.
- **SP7: Console report uses exact UX design doc Section 6 format**: The `format_console_report` output matches the box-drawing header, sectioned layout (Data, Configuration, Skills, Gaps/Get Started, Next Steps) from `docs/designs/setup-flow-ux.md` Section 6.

- **SP8: Orchestrator step dispatch uses name-based routing**: Each step has a different function signature (some take `data_dir`, some `db_url`, some `repo_root`). Rather than forcing a uniform interface, `_dispatch_step()` routes by step name to call each function with its expected args. This avoids adding adapter wrappers to every setup module.
- **SP8: `_readiness_report` stashed on context via dynamic attr**: The readiness step produces a `ReadinessReport` (not `OperationReport`) that `auto_repair` needs as input. Rather than adding a typed field for this one-step handoff, it's stashed as a dynamic attribute on `SetupContext`. This keeps the context class clean for the 12 steps that don't need it.
- **SP8: Dependencies checked but not enforced as hard errors**: `_check_dependencies()` is a no-op placeholder because the orchestrator runs steps in strict order anyway. Dependencies are documented in the step registry for future parallel execution but don't block today's sequential flow.
- **SP8: CLI command placed in `linkedout/commands/setup.py`**: Follows the existing pattern of one file per command in the `commands/` directory. Thin wrapper — all logic in `orchestrator.py`.

- **SP9: Tests placed at `tests/installation/` (repo root)**: Per the sub-phase spec, installation tests are NOT in `backend/tests/` — they test the full system across multiple packages. This matches the spec's rationale that these are integration tests too heavy for every CI push.
- **SP9: Secret detection uses regex patterns, not exact matches**: Log scanning uses `sk-[A-Za-z0-9\-_]{20,}` for OpenAI keys and similar patterns. This catches current key formats and is resistant to minor format changes.
- **SP9: No real subprocess calls in tests**: All tests mock `subprocess.run` when testing CLI integration behavior. The test suite validates logic, state management, and error handling — not actual PostgreSQL or CLI tool execution.
- **SP9: `requires_postgres` marker lazy-evaluated at module import**: PostgreSQL availability is checked once via `pg_isready` at module import time and cached. Tests requiring a real database use `@requires_postgres` to skip gracefully.

## Phase 10: Upgrade
- **SP2: `_pg_version` uses `db_session_manager` not `SessionLocal`**: Prior run created version.py with `from common.database import SessionLocal` which doesn't exist in this codebase. Corrected to `from shared.infra.db.db_session_manager import DbSessionType, db_session_manager` which is the established pattern for database sessions.
- **SP2: Most deliverables pre-existed from prior run**: VERSION file, version.py, commands/version.py, and tests were already created by a prior sub-phase run. This run verified correctness, fixed the database import, and confirmed all 15 tests pass.
- **SP4: Separate `report.py` and `logging.py` instead of putting everything in `upgrader.py`**: The spec suggested placing report structures in `upgrader.py` or a dedicated `report.py`. Chose separate `report.py` (data structures + persistence) and `logging.py` (logger factory + step timing) to keep upgrader.py focused on orchestration when sub-phase 05 implements it.
- **SP4: `counts` as a computed `@property`, not a stored field**: `UpgradeReport.counts` derives `total_steps/succeeded/skipped/failed` from the `steps` list on access rather than storing them. Eliminates stale-count bugs when steps are added after construction.
- **SP4: Reuses existing `shared.utilities.metrics` patterns**: The metrics JSONL format and directory structure match the Phase 3 `record_metric()` conventions, but `write_upgrade_report()` writes the event directly (instead of calling `record_metric()`) to keep the upgrade module self-contained and avoid coupling to the shared metrics API.
- **SP5: `UpgradeError` raised after `log_step` context manager exits, not inside it**: The `log_step` context manager sets `ctx.result` in its `except`/`else` branches, so `ctx.result` is `None` inside the `with` block. All upgrade step methods set `ctx.status = 'failed'` and `ctx.detail` inside the block, then check `ctx.result.status` after the block to raise `UpgradeError` with the populated step result. This avoids Pyright type errors and ensures correct timing in the step result.
- **SP5: `run_version_scripts` catches exceptions from `run_version_migrations` and sets `ctx.status` instead of re-raising**: Since `log_step` needs exceptions to propagate to enter its `except` branch (which would set a second step result), the pattern uses try/except to catch script failures, sets `ctx.status = 'failed'`, and lets `log_step`'s `else` branch handle the result. The `UpgradeError` is raised after the `with` block.
- **SP5: Post-upgrade health check is non-blocking**: Failures in `post_upgrade_check` do not raise `UpgradeError`. The step status remains `success` even when `linkedout status --json` returns non-zero. This matches the UX doc behavior where health check issues are warnings, not upgrade failures.
- **SP5: CLI upgrade command in `commands/upgrade.py`, not `dev_tools/cli.py`**: The sub-phase spec said to modify `dev_tools/cli.py`, but the actual CLI lives in `linkedout/cli.py` using `linkedout/commands/*.py` modules. Created `commands/upgrade.py` following the established pattern and registered it in `cli.py`'s lazy loader.
- **SP5: Skipped integration tests (test_upgrade_with_migration.py, test_upgrade_report.py)**: These require Alembic migration infrastructure and a real database. The sub-phase spec lists them, but they're better suited for a separate integration test run. All unit tests (128 total across all upgrade modules) pass.
- **SP4: Correlation ID format is `cli_upgrade_YYYYMMDD_HHMM`**: Follows the CLI correlation ID convention from `docs/decision/logging-observability-strategy.md` (section 8). Uses UTC timestamp, not nanoid, because upgrade invocations are infrequent and the timestamp doubles as a human-readable identifier.

- **SP3: Used `packaging.version.Version` for semver comparison**: `packaging` is a transitive dependency (always available via pip) rather than a direct project dependency. Chose this over a hand-rolled parser for robustness with PEP 440 edge cases (pre-releases, dev builds). Falls back to `is_outdated=False` on `InvalidVersion` to avoid blocking users.
- **SP3: `httpx` for HTTP, matching existing project dependency**: Project already has `httpx>=0.25.0` in requirements.txt. Used `httpx.Client` (sync) with a 10-second timeout. The `with` block ensures proper connection cleanup.
- **SP3: `UpdateInfo` is a frozen dataclass, not a dict**: Provides type safety and immutability for cached results. `asdict()` handles JSON serialization for the cache file.

- **SP6: Snooze logic added directly to `update_checker.py` rather than a separate file**: The spec suggested extending `update_checker.py` from sub-phase 03. Kept all snooze functions (`is_snoozed`, `snooze_update`, `reset_snooze`) in the same file since they're tightly coupled to `check_for_update()` and share the same state directory.
- **SP6: Auto-upgrade (`try_auto_upgrade`) only does git pull + dep install, not migrations**: Per the spec note, auto-upgrade should not run migrations silently. `try_auto_upgrade` pulls code and updates deps only. Migrations are logged but not auto-applied. This keeps the v1 implementation safe.
- **SP6: `_maybe_reset_snooze` extracted as a helper**: Instead of inlining the snooze-reset logic in `check_for_update()`, extracted it as `_maybe_reset_snooze()` for clarity. It reads the snooze file once to check if the snoozed version differs from the detected latest version.
- **SP6: `subprocess` imported inside `try_auto_upgrade` body**: Since auto-upgrade is rarely invoked, subprocess is imported lazily inside the function to avoid unnecessary import overhead on every `update_checker` import.

## Phase 11: Query History
- **SP2: `format_stat_line` label width fixed at 24 chars**: Chose 24-character fixed-width for the label field in `format_stat_line`. This accommodates most label lengths (e.g. "Avg response time") while keeping output compact. Labels longer than 22 characters (plus colon and space) will push the value column right, but alignment remains consistent across lines with similar-length labels.
- **SP2: `format_table` separator line not capped at `max_col_width`**: The separator row uses dashes matching the computed column width (which respects `max_col_width` for data/header cells). The separator itself is `col_width + 2` characters (for padding spaces) per cell, which is structural — not content — so it intentionally exceeds `max_col_width` by 2.
- **SP2: Numeric column detection is strict**: A column is only right-aligned if ALL non-empty data cells parse as float. Mixed columns (e.g. "100", "abc", "200") fall back to left-alignment. This avoids surprising alignment when a column happens to have a few numeric-looking values.
- **SP2: `format_count` uses Python's built-in `f"{n:,}"` formatting**: No locale-dependent formatting (e.g. `locale.format_string`). Commas are always used as thousand separators regardless of system locale, which ensures consistent output across environments.

- **SP1: Phase 3 logging/metrics imported with ImportError fallback**: `shared.utilities.logger.get_logger` and `shared.utilities.metrics.record_metric` don't exist yet (Phase 3 modules not in the OSS codebase). The query_logger catches `ImportError` and falls back to stdlib `logging.getLogger(__name__)` for logging and skips metrics recording entirely. This matches the spec's "if not available, skip gracefully" requirement.
- **SP1: `_logger.info()` uses `extra={}` kwarg for structured fields**: Since the fallback is stdlib logging (not loguru), structured fields (`query_id`, `query_type`, etc.) are passed via `extra={}` rather than loguru's `bind()` pattern. This is compatible with both stdlib and loguru (via the stdlib intercept).
- **SP1: Multiprocessing test uses top-level worker function**: `_worker_write_entries` is defined at module level (not inside the test class) because `multiprocessing.Pool.map` requires picklable targets. The worker sets `LINKEDOUT_DATA_DIR` via `os.environ` in each child process since `monkeypatch` doesn't propagate across process boundaries.

- **SP5: Skill template replaces Phase 8 stub entirely**: The Phase 8 stub was a placeholder directing users to Phase 11. The full template now implements all five steps (diagnostics, health scoring, recommendations, historical comparison, report persistence) as AI-host instructions rather than Python code. The skill is a pure "prompt program" — it tells the AI host what to do step by step, with no backend module dependency beyond `linkedout diagnostics --json` and `linkedout status --json`.
- **SP5: Health scoring thresholds match `format_health_badge` in formatters.py**: The skill documents the same thresholds (>=90+0 issues = HEALTHY, >=70 = WARNING, <70 = CRITICAL) as the `format_health_badge` function from SP2. The skill references the algorithm inline rather than calling the Python function, since skills run in the AI host context (not Python directly).
- **SP5: No separate Python module for setup-report**: Unlike sp1 (query_logger) and sp2 (formatters), the setup-report skill has no dedicated Python module. All intelligence (health scoring, recommendations, comparison) is encoded in the skill template instructions. This is intentional — the skill wraps existing CLI commands (`linkedout diagnostics --json`, `linkedout status --json`) and adds AI-native analysis on top. Adding a Python module would duplicate what the AI host does naturally.
- **SP5: ROUTING.md.tmpl already registered**: The Phase 8 skill stub already registered `/linkedout-setup-report` in `skills/routing/ROUTING.md.tmpl`. No routing changes were needed for SP5.

- **SP3: Skill is a pure "prompt program" with embedded Python one-liners**: The `/linkedout-history` skill has no dedicated Python module. It instructs the AI host to read JSONL files directly and provides inline Python one-liners for parsing, grouping, and display. This matches the architecture decision that skills are reader-side logic while `query_logger.py` (SP1) is the write-side. No new backend dependency needed.
- **SP3: Session grouping uses `session_id` from JSONL, not `.active_session.json`**: The history skill groups queries by `session_id` field in the JSONL entries, not by reading the session manager's active session file. This is read-only and works on historical data (`.active_session.json` only tracks the current session). The `session_id` field was designed for exactly this purpose in SP1.
- **SP3: Text search is case-insensitive substring match on `query_text` only**: Intentionally simple — no fuzzy matching, no regex, no searching across `query_type` or other fields. Users searching "Stripe" will find "who do I know at Stripe?" but not a query that returned Stripe in results. This matches the spec's "case-insensitive substring match" requirement.
- **SP3: ROUTING.md.tmpl already registered**: Phase 8 stub already registered `/linkedout-history` with trigger patterns. No routing changes needed for SP3.

- **SP4: Report skill is a "prompt program" with embedded bash/Python**: Like SP3 and SP5, the `/linkedout-report` skill has no dedicated Python module. It instructs the AI host to gather data from multiple sources (JSONL files, DB queries, CLI commands) and compose a formatted report. The formatting conventions from `formatters.py` (SP2) are referenced in the output formatting guidelines but the skill doesn't import the module — the AI host formats inline.
- **SP4: `linkedout status --json` preferred over direct DB for Network Stats**: The skill tries the CLI first for Network Stats (section 3) since `linkedout status --json` already aggregates the key metrics. Falls back to direct psql queries if the CLI is unavailable. This avoids duplicating the status logic.
- **SP4: ROUTING.md.tmpl already registered**: Phase 8 stub already registered `/linkedout-report` with trigger patterns. No routing changes needed for SP4.
- **SP4: JSON output mode is AI-interpreted, not flag-based**: Since skills are prompts (not CLIs), the JSON output mode is triggered by the user saying "as JSON" or similar in natural language, not by a `--json` flag. The skill documents the expected JSON schema structure for the AI host to follow.

- **SP6: Integration tests are file-based, not DB-backed**: All integration tests use `tmp_path` + `LINKEDOUT_DATA_DIR` env var for isolation. No database fixtures needed since query history is JSONL-based and report aggregation reads from flat files. This matches the Phase 11 architecture where all query data is file-based.
- **SP6: Metrics integration test uses `pytest.skip` for graceful degradation**: The `test_query_triggers_metric_recording` test checks if the metrics module is wired (daily JSONL files created) and skips if not. This handles environments where `shared.utilities.metrics.record_metric` import succeeds but `LINKEDOUT_METRICS_DIR` points elsewhere.
- **SP6: Report aggregation tests verify data patterns, not skill execution**: Since skills are "prompt programs" (AI-host instructions, not Python modules), the report aggregation tests verify the data structures and aggregation patterns the skills rely on — JSONL parsing, query type distribution, import report parsing, graceful degradation with missing data, and report JSON persistence with historical comparison. They do not execute the skill templates themselves.
- **SP6: `record_metric` context kwargs nested under `metadata` key**: The metrics test accounts for `record_metric` storing keyword arguments as top-level JSONL keys. In query_logger, `metadata={...}` is passed as a kwarg, so the JSONL output has `"metadata": {"query_type": ...}` (nested), not `"query_type"` at top level. The test uses `metric.get('metadata', metric)` to handle both patterns.
- **SP6: Exit criteria verification script not committed as file**: Per the sub-phase spec ("does not need to be committed unless the runner decides to"), the exit criteria were verified inline during the test run rather than as a committed bash script.

- **SP7: `fetch_expected_checksum` as a separate function**: The sub-phase spec mentions the checksum "may be embedded in the release description or as a separate `.sha256` file". Chose a separate `.sha256` asset file approach (e.g., `linkedout-extension-v0.2.0.zip.sha256`) since it's simpler to parse and a standard convention. If the file is unavailable, the function returns `None` and the download is accepted without checksum verification.
- **SP7: Extension directory non-empty check for `check_extension_installed()`**: The spec says "check for directory or extension config marker". Chose checking if `~/linkedout-data/extension/` exists AND is non-empty (has any files). An empty directory could exist from a failed download or manual cleanup, so non-empty is a stronger signal of prior installation.
- **SP7: Extension update step placed between post-upgrade check and What's New**: The UX design doc (Section 3, Step 7) shows the extension update between version migration scripts and post-upgrade health check. However, in the upgrader implementation, `post_upgrade_check` is already in the blocking step sequence. The extension step is placed after the step sequence (non-blocking) and before What's New, matching the spec's requirement that extension failures don't stop the upgrade.
- **SP7: Checksum mismatch treated as `failed` step, not raised exception**: When checksum verification fails, the extension step result has `status='failed'` but no exception is raised. This keeps the extension update non-blocking — the core upgrade succeeds even if the downloaded file is corrupt.

## Phase 12: Extension
- **SP2: `VITE_BACKEND_URL` set on both `wxt build` and `wxt zip` steps**: Vite env vars are resolved at build time. Both the `build` and `zip` steps receive `VITE_BACKEND_URL=http://localhost:8001` to ensure consistent baking regardless of which step resolves the import.
- **SP2: Release asset renamed from WXT default to `linkedout-extension-{version}.zip`**: WXT zip produces a file with its own naming convention (`linkedout-extension-0.0.0-chrome.zip`). The workflow extracts the version from the release tag (`v1.0.0` → `1.0.0`), copies the zip to a clean `linkedout-extension-{version}.zip` name, and uploads that as the release asset. This gives users a predictable download URL.
- **SP2: `workflow_dispatch` uploads as workflow artifact, not release asset**: Per sub-phase spec, manual triggers have no release to attach to. The zip is uploaded as a GitHub Actions artifact instead, downloadable from the workflow run page.
- **SP2: `.gitignore` already covers `extension/.output/`**: The root `.gitignore` has `.output/` (no leading `/`), which matches anywhere in the tree including `extension/.output/`. Verified with `git check-ignore`. No change needed.

- **SP4: SIGTERM before SIGKILL for port cleanup**: Changed `_kill_port()` from immediate SIGKILL to SIGTERM first with 10s wait, then SIGKILL fallback. More graceful — gives the process a chance to clean up.
- **SP4: CLI flags default to None, fall through to config**: `--port` and `--host` CLI options default to `None` so the precedence chain works correctly: CLI flag > env var > config YAML > code default. The `get_config()` singleton already resolves env vars and YAML.
- **SP4: Background mode uses `sys.executable -m uvicorn` instead of bare `uvicorn`**: Ensures the correct Python interpreter is used (the one running the CLI), avoiding issues where `uvicorn` on PATH might point to a different Python installation.
- **SP4: PYTHONPATH injected for background subprocess**: Background mode passes `PYTHONPATH=backend/src` so `main.py` can resolve `shared.*` and `linkedout.*` imports correctly in the forked process.
- **SP4: Foreground mode writes PID file too**: Even in foreground mode, the PID is written so `linkedout status` can detect it. The PID file is cleaned up in a `finally` block.
- **SP4: `stop-backend` sends SIGTERM first, SIGKILL after 10s**: Follows the spec exactly. Clean shutdown path avoids zombie processes.
- **SP4: `status` uses both PID check and health endpoint**: PID file check detects if the process is alive. Health endpoint check confirms it's actually serving. If PID exists but health fails, still reports "running" (might be starting up).

- **SP3: Enrichment mode merged into unified `linkedout_config` storage key**: The pre-existing `settings.ts` stored enrichment mode as a separate top-level key (`enrichmentMode`) in `browser.storage.local`. Merged it into the `linkedout_config` object alongside all other settings. `settings.ts` now delegates to `config.ts`. A one-time migration in `initConfig()` moves the legacy key to the new location.
- **SP3: Voyager decoration IDs and `MUTUAL_PAGE_SIZE` kept in `constants.ts`**: These are LinkedIn internal API identifiers that must match LinkedIn's current API exactly — they are NOT user-configurable. Only the system IDs (`TENANT_ID`, `BU_ID`, `APP_USER_ID`) were moved to config since those are options-page configurable.
- **SP3: `getHeaders()` function replaces static `headers` object in `client.ts`**: The old pattern created headers at module load time with a static `APP_USER_ID`. Since user ID is now configurable via the options page, headers must be computed per-request via a function.
- **SP3: Storage change listener auto-invalidates config cache**: Both an explicit `onConfigChange()` subscriber and a global listener ensure `getConfigSync()` always reflects the latest options page saves without requiring an extension reload.
- **SP3: Options page uses inline styles matching sidepanel design**: Follows the same font (Fraunces, Fragment Mono) and color scheme (#1B1B18, #FDFBFE, #E8E5E0) as the existing sidepanel for visual consistency.

## Phase 12: Chrome Extension Add-on — SP5: Extension Logging

- **Renamed `devLog.ts` → `dev-log.ts`**: The spec names the file `extension/lib/dev-log.ts` (kebab-case). The pre-existing `devLog.ts` was not imported anywhere, so renamed to match spec and kebab-case convention used by other files in `lib/` (e.g., `rate-limiter.ts`).
- **`devLog()` kept async despite "zero-cost" spec note**: The function is async because checking `LINKEDOUT_DEBUG` in `browser.storage.local` is inherently async. In production builds, `import.meta.env.DEV` short-circuits to false synchronously (dead-code eliminated by the bundler), and the cached `debugEnabled` flag avoids repeated storage reads. The "zero-cost" goal is met via bundler tree-shaking of the DEV branch.
- **Backend client `request()` logs errors to activity log directly**: Rather than routing through `appendLogAndNotify` (which is only available in background.ts), the client calls `appendLog()` directly for error cases. This keeps error logging co-located with the error path and avoids coupling the HTTP client to the background script's messaging system.
- **Error badge increments via `appendLogAndNotify` hook**: Instead of adding `incrementErrorBadge()` at every error site in background.ts, the badge increments inside `appendLogAndNotify()` when `entry.action === 'error'`. This ensures all logged errors are counted without missing any call sites.
- **Rate limiter `canProceed()` return type unchanged**: Rather than changing the return type to a richer object (which would break callers), devLog calls were added inside `canProceed()` itself per the spec's directive to "add them using `devLog()`". This keeps the rate limiter API simple.

## Phase 12: Chrome Extension Add-on — SP6: Extension Setup Skill

- **Template path follows codebase convention, not spec literal**: The spec references `skills/templates/linkedout-extension-setup.md.tmpl`, but the actual codebase pattern (used by `bin/generate-skills`) discovers templates at `skills/<skill-name>/SKILL.md.tmpl`. Created template at `skills/linkedout-extension-setup/SKILL.md.tmpl` to match the established pattern.
- **Nested zip extraction handling added**: The UX design doc doesn't address the case where a zip extracts with a nested subdirectory (common with GitHub-built zips). Added a `manifest.json` existence check and automatic flattening step after extraction to handle this gracefully.
- **macOS Chrome detection path included**: Added `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` detection alongside Linux `google-chrome`/`google-chrome-stable`/`chromium-browser` to ensure cross-platform Chrome version checking works.

## Phase 12 SP7: Extension Documentation

- **Architecture diagram expanded beyond plan template**: The shared context's architecture diagram was a simplified ASCII sketch. The actual implementation has a richer architecture (MAIN world content script, ISOLATED world bridge relay, service worker orchestration, options page). The documentation diagram was expanded to reflect the real codebase with all four entrypoints and their specific roles, rather than using the simplified version from the plan.
- **Enrichment mode documented in config reference**: The options page exposes an enrichment mode toggle (manual/auto) that was not listed in the sub-phase's config reference table. Added it since it's a user-configurable setting visible in the options page.
- **Voyager search decoration ID documented**: `constants.ts` has a second decoration ID (`VOYAGER_SEARCH_DECORATION_ID` for mutual connection extraction) beyond the one mentioned in the sub-phase spec. Both are documented in the fragility section context.
- **Runtime storage mechanism documented**: Added a section explaining that extension config is persisted in `browser.storage.local` under the `linkedout_config` key with instant change propagation — this implementation detail is important for troubleshooting.

## Phase 13: Polish & Launch

### SP1: Public Roadmap (13G)
- **GitHub Projects board skipped**: `gh auth` tokens are invalid, so the GitHub Projects board could not be created via CLI. Per the sub-phase instructions, ROADMAP.md is the primary deliverable and the board is a nice-to-have. Manual creation steps: create a Kanban board titled "LinkedOut OSS Roadmap" with columns Done/Up Next/Future/Community Requests, populate from ROADMAP.md content.
- **Added 2 extra Future items beyond plan**: Added "Advanced analytics" and "Plugin system" to the Future section beyond the 5 items in the plan, bringing total roadmap items to 23 (well above the 10-item minimum).
- **Community Requests links to issue template URL**: Used a direct GitHub new-issue URL with `feature-request` label pre-filled rather than just text instructions, making it easier for users to act on.

### SP2: Documentation Polish (13C)
- **Existing docs preserved and extended**: `docs/extension.md` and `docs/configuration.md` already existed with comprehensive content from prior phases. Rather than rewriting, added the specific sections the spec required (Setup with Chrome sideloading steps, Usage section for extension.md; CI/automation section for configuration.md).
- **Langfuse kept in configuration.md**: The shared context prohibits Langfuse references in public docs, but the configuration reference table must include it since it's a real configurable setting. It's marked "Optional" and "Off by default", which aligns with the "mention only as optional if relevant" guidance.
- **"Docker-free" wording removed from configuration.md**: The CI/automation section originally said "Docker-free test environments" — removed the Docker reference per prohibitions.
- **No `linkedout serve` in docs**: The cli-surface.md mentions both `start-backend` and `serve` in different places. Used `start-backend` consistently since that's what appears in the command inventory table and README.

### SP3: Test Suite & Observability Validation (13D + 13E)
- **Implemented `linkedout diagnostics` command**: Was a Phase 3 stub ("Not yet implemented -- coming in Phase 3"). All building blocks existed (health_checks.py, get_db_stats, etc.), so implemented the full diagnostics command with `--json`, `--repair`, and report saving. This was a necessary gap fill for release readiness.
- **Added OperationReport + metrics to `import-connections`**: Was missing report save and report path display. Quick fix — all patterns existed from `embed` and `import-seed` commands.
- **Added OperationReport + metrics + @cli_logged to `import-contacts`**: Was the least-observability-compliant CLI command — missing logging, metrics recording, operation report, and the @cli_logged decorator. All added inline.
- **Added OperationReport to `compute-affinity`**: Had metrics but no report save. Added inline.
- **Added `record_metric` to `import-seed`**: Had OperationReport but no metrics JSONL recording. Added inline.
- **CI workflow expanded significantly**: Original ci.yml only had `lint` and `typecheck` jobs. Added unit test job (Tier 2), integration test job with pgvector PostgreSQL service (Tier 3), nightly installation test matrix (Tier 4, Ubuntu + macOS), and extension build check. Used `pgvector/pgvector:pg16` image for pgvector support in integration tests.
- **Installation test `services:` only works on ubuntu runners**: GitHub Actions `services:` containers require Linux runners. The macOS matrix entry for installation tests won't have the PostgreSQL service container, but `@requires_postgres` tests are optional and skipped when PG is unavailable.
- **tests/README.md includes Tier 5 (Skills)**: The sub-phase spec listed 4 tiers. Added Tier 5 for the `tests/skills/` tests since they exist and run in CI. Also added Tier 6 (LLM Eval) as informational.
- **validate-observability.sh uses code-level checks**: Rather than requiring a running instance (which may not exist during CI), the script validates observability by grepping the source code for expected patterns (log file configs, correlation ID patterns, metric recording, etc.). All 51 checks pass.

### SP4: Multi-Platform CI & E2E Testing (13A + 13B)
- **Moved installation tests out of ci.yml into dedicated installation-test.yml**: The existing ci.yml had a Tier 4 installation-test job using Docker `services:` — which only works on Linux runners. The new `.github/workflows/installation-test.yml` installs PostgreSQL natively on both Ubuntu and macOS, enabling true multi-platform coverage. Removed the redundant job and nightly schedule from ci.yml.
- **Native PostgreSQL installation on macOS**: macOS GitHub Actions runners cannot use Docker container `services:`. The workflow installs PostgreSQL via `brew install postgresql@{version}` with `initdb`/`pg_ctl start` for macOS, and via the official PostgreSQL APT repository for Ubuntu. This enables the full 12-combination matrix.
- **sample-seed.json instead of sample-seed.sqlite**: The sub-phase spec suggested `sample-seed.sqlite`, but the project uses PostgreSQL exclusively (not SQLite for data). Created `tests/fixtures/sample-seed.json` matching the existing seed data JSON format used by `mock_github_releases` conftest fixture.
- **Idempotency/recovery/degraded tests only on Ubuntu**: These tests are implementation-detail tests that don't vary by platform. Running them on all 12 matrix combinations wastes CI minutes with no additional coverage.
- **E2E job gated on matrix jobs passing**: The `e2e` job uses `needs: installation-matrix` to run only after all 12 matrix jobs pass. Uses ubuntu-24.04 + Python 3.12 + PG 17 as the representative combination.
- **Upgrade path test handles no-tags gracefully**: Since v0.1.0 is the first release, there may be no prior tags. The test falls back to a HEAD-only verification (migrations clean, CLI functional) when no base tag exists.
- **Removed stale `if: github.event_name != 'schedule'` conditions from ci.yml**: After removing the nightly schedule trigger from ci.yml, cleaned up the conditional guards on test/integration/extension jobs that were skipping on schedule runs.

## Phase 13 SP5: Good First Issues
- **Updated file paths to point to actual command files**: The sub-phase spec referenced `backend/src/linkedout/cli.py` for all CLI issues, but commands are implemented in individual files under `backend/src/linkedout/commands/` (e.g., `status.py`, `diagnostics.py`, `config.py`). Updated issue descriptions to reference the specific command files for accuracy while keeping `cli.py` as the entry point reference.
- **Script-based issue creation due to gh auth failure**: GitHub CLI tokens were invalid at execution time. Created `scripts/create-good-first-issues.sh` with all 8 issues ready to run after `gh auth login`. Human action required to authenticate and execute the script.

## Phase 13 SP6: v0.1.0 Release
- **Added `workflow_call` trigger to ci.yml**: The release workflow gates on CI passing via `uses: ./.github/workflows/ci.yml`. This requires ci.yml to support the `workflow_call` trigger, which was not present. Added it as a third trigger alongside `push` and `pull_request`.
- **Release workflow uses `gh release create` instead of `softprops/action-gh-release`**: The sub-phase spec suggested the `softprops/action-gh-release@v2` action. Used `gh release create` instead — it's GitHub's own CLI, avoids third-party action dependency, and handles asset collection more flexibly (conditional seed data attachment).
- **Seed data not attached to main release**: Seed SQLite files are not in the git repo (too large). They're released separately via `release-seed-data.yml`. The main release workflow conditionally attaches seed files if present but doesn't fail when they're absent.
- **Extension `VITE_BACKEND_URL` env var required for build**: The WXT build needs `VITE_BACKEND_URL` set (matching the extension-build.yml workflow). Added to the release workflow's extension build step.
- **Tag/release commands documented, NOT executed**: Per sub-phase instructions, the actual `git tag` + `git push` commands are documented for SJ to run manually. Release timing is SJ's decision.
