# SP4: Rewrite Test Fixture + File Changes

**Phase:** 3a-3b — Test Fixtures + Tests
**Sub-phase:** 4 of 6
**Dependencies:** SP3 (import rewrite must be complete — test fixture mirrors the new import format)
**Estimated effort:** ~45 minutes
**Shared context:** `_shared_context.md`

---

## Scope

Complete rewrite of the seed test fixture generator: replace hand-written SQLite DDL + synthetic data with PostgreSQL + pg_dump pattern. Also handle file additions/deletions and .gitignore updates.

**Plan section:** Phase 3, tasks 3a and 3b

---

## Inputs

- `backend/tests/fixtures/generate_test_seed.py` (current: 644 lines, hand-written SQLite DDL)
- `backend/tests/fixtures/test-seed-core.sqlite` (current fixture, to be deleted)
- Entity classes for schema metadata

## Outputs

- `backend/tests/fixtures/generate_test_seed.py` — complete rewrite
- `backend/tests/fixtures/test-seed-core.dump` — new fixture (committed)
- `backend/tests/fixtures/seed-manifest.json` — new fixture (committed)
- `backend/tests/fixtures/test-seed-core.sqlite` — deleted
- `.gitignore` — updated

---

## Task 1: Rewrite generate_test_seed.py

**File:** `backend/tests/fixtures/generate_test_seed.py` — complete rewrite

### Current state
- 644 lines of hand-written SQLite DDL + synthetic data
- Creates 10 tables (6 seed + 4 profile) in SQLite
- Hand-written DDL (270 lines) duplicates and may drift from entity metadata
- Writes booleans as INTEGER 0/1, arrays as JSON strings, timestamps as ISO strings

### New approach
Uses PostgreSQL + pg_dump, mirrors Phase 1 export pattern. Only creates 6 seed tables (profile tables are not part of seed pipeline).

### New flow
1. Create temp database `linkedout_seed_test_fixture` (same pattern as demo fixture's `_psql()` helper)
2. `CREATE SCHEMA _seed_staging`
3. Create 6 seed tables in staging using `Base.metadata.create_all(engine, tables=[...])` — filtered to only seed-relevant entities. This prevents DDL drift.
4. Insert synthetic data in **native PG types**:
   - Booleans: `TRUE`/`FALSE` (not INTEGER 0/1)
   - Arrays: `ARRAY['linkedin', 'pdl']` (not JSON strings)
   - Timestamps: `TIMESTAMPTZ` (not ISO text strings)
5. `pg_dump -Fc --schema=_seed_staging --no-owner` → `test-seed-core.dump`
6. Generate `seed-manifest.json` alongside (table counts, version `"0.0.1-test"`, format `"pgdump"`)
7. Drop temp database

### Synthetic data counts (preserved from current fixture)
- 10 companies across industries (Technology, Finance, Healthcare, ...), split SF/NY
- 15 company aliases cycling across companies, alternating linkedin/manual source
- 10 role aliases: SWE, Sr SWE, PM, Sr PM, DS, ML Eng, VP Eng, CTO, Designer, DevOps
- 8 funding rounds across 4 companies (co_test_001-004), Seed + Series A each
- 5 startup tracking records (co_test_001-005), 3 watching + 2 not
- 12 growth signals cycling across 5 companies, alternating headcount/revenue

### Same IDs preserved
- `co_test_001` through `co_test_010` for companies
- Other IDs following same patterns as current fixture

### Important changes from current fixture
- **No profile/experience/education/skill data** — those tables are not part of seed pipeline
- All data in native PG types (no SQLite type conversion)
- Uses `Base.metadata.create_all()` for DDL instead of hand-written CREATE TABLE

### Note
This script requires PostgreSQL + pg_dump locally. The fixture is committed — regeneration is rare (only on schema change). Uses the same `_psql()` helper pattern as `generate_test_demo_dump.py`.

---

## Task 2: File Changes

### Delete
- `backend/tests/fixtures/test-seed-core.sqlite`

### Add (commit these)
- `backend/tests/fixtures/test-seed-core.dump` (generated by Task 1)
- `backend/tests/fixtures/seed-manifest.json` (generated by Task 1)

### Update `.gitignore`
- Swap `!backend/tests/fixtures/test-seed-core.sqlite` → `!backend/tests/fixtures/test-seed-core.dump`
- Ensure `!backend/tests/fixtures/seed-manifest.json` is tracked if needed

---

## Verification Checklist

- [ ] `generate_test_seed.py` uses `Base.metadata.create_all()` instead of hand-written DDL
- [ ] No hand-written CREATE TABLE statements remain
- [ ] Fixture creates exactly 6 seed tables (no profile/experience/education/skill tables)
- [ ] All data in native PG types (booleans, arrays, timestamps)
- [ ] Same data counts: 10 companies, 15 aliases, 10 role aliases, 8 funding rounds, 5 tracking, 12 signals
- [ ] Same IDs: `co_test_001` through `co_test_010`
- [ ] `test-seed-core.dump` exists in `backend/tests/fixtures/`
- [ ] `seed-manifest.json` exists in `backend/tests/fixtures/` with version `"0.0.1-test"` and format `"pgdump"`
- [ ] `test-seed-core.sqlite` is deleted
- [ ] `.gitignore` updated: `.sqlite` exclusion → `.dump` exclusion
- [ ] `pg_restore --list backend/tests/fixtures/test-seed-core.dump` shows 6 tables in `_seed_staging` schema
